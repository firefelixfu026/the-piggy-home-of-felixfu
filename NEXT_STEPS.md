# 下一步开发计划

当前项目状态：已经完成本地 MVP、FastAPI 接口骨架、小游戏嵌入、小游戏 iframe 响应式修正、PostgreSQL 数据持久化和基础文档；本地 Git 仓库已绑定 GitHub 远程仓库 `https://github.com/firefelixfu026/the-piggy-home-of-felixfu.git`。  
GitHub 推送问题已通过 Clash `127.0.0.1:7897` 代理解决，排查细节见 [GITHUB_PUSH_TROUBLESHOOTING.md](./GITHUB_PUSH_TROUBLESHOOTING.md)。

## 1. 立即要做的事情

### 你需要做

1. 保持 Clash 运行，当前 Git 推送依赖 `127.0.0.1:7897`。
2. 打开本地博客检查小游戏页是否符合预期：`http://127.0.0.1:5173`。
3. 确认本地数据库和后端能正常启动，验证命令见 [DATABASE.md](./DATABASE.md)。
4. 确认下一步是否优先做“文章管理后台”，也就是让管理员可以发布、编辑、删除文章。

### 我可以辅助完成

1. 使用 Clash 代理继续执行后续 `git push`。
2. 检查 GitHub 仓库里的文件结构是否完整。
3. 继续实现 v0.4.0：文章管理后台 + 管理员文章 CRUD。
4. 更新版本日志和代码分析文档，保持答辩材料可用。
5. 如果 Clash 端口变化，我可以更新 Git 代理配置并重试推送。

## 2. 已完成版本：v0.3.0 数据持久化版本

数据库持久化已经完成。文章、评论和互动计数已从静态/内存数据迁移到 PostgreSQL。接入数据库后，后续登录注册、管理后台、评论审核、点赞收藏统计、AI 总结缓存都有了稳定基础。

v0.3.0 已完成：

- 使用 PostgreSQL 作为正式数据库。
- 使用 SQLAlchemy 作为后端 ORM。
- 创建数据库表：用户、文章、评论、互动、标签。
- 把 `/api/articles` 从内存数据改成数据库读取。
- 增加文章详情接口、评论提交接口和互动提交接口的数据库写入。
- 增加初始化种子数据，让本地环境启动后自动有示例文章。
- 增加 `docker-compose.yml`，一键启动 PostgreSQL。
- 更新 README，写清楚数据库启动、迁移和本地验证方式。

## 3. 推荐下一版本：v0.4.0 文章管理后台

### 你应该做

- 确认后台入口放在哪里，例如导航栏“管理”页。
- 确认只有你一个管理员，还是后续需要多管理员。
- 确认文章编辑格式：先用普通文本/Markdown 文本框，还是直接做富文本编辑器。

### 我可以辅助完成

- 新增文章创建、编辑、删除接口。
- 新增前端管理页。
- 实现文章标题、摘要、正文、标签、阅读时长、发布日期编辑。
- 接入 Markdown 编辑和预览。
- 增加基础表单校验。
- 更新 `CHANGELOG.md` 和 `CODE_ANALYSIS.md`。
- 提交代码并推送 GitHub。

## 4. v0.3.0 后的下一步

建议按这个顺序继续：

1. v0.4.0：文章管理后台，支持管理员发布、编辑、删除文章。
2. v0.5.0：邮箱注册登录和基础 JWT 鉴权。
3. v0.6.0：GitHub OAuth 登录。
4. v0.7.0：Docker Compose 一键启动前端、后端、数据库。
5. v0.8.0：GitHub Actions 自动构建和测试。
6. v0.9.0：云服务器部署、Nginx、域名和 HTTPS。
7. v1.0.0：AI 每日技术新闻和文章 AI 总结。

## 5. 现在的执行建议

建议你先完成两件事：

1. 在本地博客里检查文章、评论、点赞、收藏、点踩是否符合预期。
2. 确认下一步是否开始 v0.4.0 文章管理后台。

如果这两点都满足，就可以直接开始 v0.4.0。
